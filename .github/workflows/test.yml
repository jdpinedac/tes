name: PruebaSinNada
on:
  workflow_dispatch:
  workflow_call:
  push:
jobs:
  With-VPN:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
